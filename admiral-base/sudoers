# /etc/sudoers
# This file MUST be edited with the 'visudo' command as root.
# See the man page for details on how to write a sudoers file.
# Defaults

Defaults  !lecture,tty_tickets,!fqdn

# Uncomment to allow members of group sudo to not need a password
# %sudo ALL=NOPASSWD: ALL

# Host alias specification

# User alias specification
User_Alias TEST_ACCESS_USER = www-data

# Cmnd alias specification
Cmnd_Alias TEST_ACCESS_CMD = /usr/local/sbin/testuseraccess.sh

# User privilege specification
root  ALL=(ALL) ALL

# Members of the admin group may gain root privileges
%admin ALL=(ALL) ALL
%RGLeader ALL=(ALL) ALL

# Selected users may run the /usr/local/sbin/testuseracess.sh script as any user without password
TEST_ACCESS_USER ALL = (ALL) NOPASSWD: TEST_ACCESS_CMD

